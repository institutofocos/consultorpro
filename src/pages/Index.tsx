
import React from 'react';
import { Dashboard } from '@/components/dashboard/Dashboard';
import { toast } from "sonner";

const Index = () => {
  // Removed auth checks and database checks
  return <Dashboard />;
};

export default Index;
